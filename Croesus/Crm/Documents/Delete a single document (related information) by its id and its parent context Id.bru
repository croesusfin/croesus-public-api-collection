meta {
  name: Delete a single document (related information) by its id and its parent context Id
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/crm/api/v2/:contextType/:contextId/Documents/:id
  body: none
  auth: inherit
}

params:path {
  contextType: 
  contextId: 
  id: 
}

headers {
  apiroute: {{apiRoute}}
  apikey: {{apiKey}}
}
