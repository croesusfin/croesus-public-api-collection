meta {
  name: Get a single note by its id and its context id
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/crm/api/v2/:contextType/:contextId/Notes/:id
  body: none
  auth: inherit
}

params:path {
  contextType: 
  contextId: 
  id: 
}

headers {
  apiroute: {{apiRoute}}
  apikey: {{apiKey}}
}
