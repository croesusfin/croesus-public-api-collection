meta {
  name: Get a client by it's id
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/crm/api/v2/Clients/:clientId
  body: none
  auth: inherit
}

params:query {
  ~requestedCurrency: 
}

params:path {
  clientId: 
}

headers {
  apiroute: {{apiRoute}}
  apikey: {{apiKey}}
}
