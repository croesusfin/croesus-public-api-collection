meta {
  name: Get projected income positions for a given account
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/pfm/api/v1/accounts/:accountId/portfolio/projectedincome/positions
  body: none
  auth: inherit
}

params:query {
  ~requestedCurrency: 
}

params:path {
  accountId: 
}

headers {
  apiroute: {{apiRoute}}
  apikey: {{apiKey}}
}
