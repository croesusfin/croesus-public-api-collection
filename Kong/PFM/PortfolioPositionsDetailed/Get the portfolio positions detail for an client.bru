meta {
  name: Get the portfolio positions detail for an client
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/pfm/api/v1/clients/:clientId/portfolio/positions/detailed
  body: none
  auth: inherit
}

params:query {
  ~requestedCurrency: 
  ~sortOrder: 
}

params:path {
  clientId: 
}

headers {
  apiroute: {{apiRoute}}
  apikey: {{apiKey}}
}
